import type { Locale } from "@/types/common";

export const localeCookieName = "methodology-match-locale";
export const defaultLocale: Locale = "en";
export const supportedLocales: Locale[] = ["en", "ru"];

